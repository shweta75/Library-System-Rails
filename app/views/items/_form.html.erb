<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label t('items.forms.isbn') %>
    <%= form.number_field :isbn %>
  </div>

  <div class="field">
    <%= form.label t('items.forms.title') %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label t('items.forms.description') %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label t('items.forms.author') %>
    <%= form.text_field :author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
